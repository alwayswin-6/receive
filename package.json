{
  "name": "receive-screen-backend",
  "version": "1.0.0",
  "private": true,
  "description": "JavaScript WebRTC signaling backend for local device capture",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node server.js"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=18"
  }
}
