services:
  - type: web
    name: receive-screen
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python receiver_server.py"
    envVars:
      - key: HOST
        value: 0.0.0.0
